# Implementation Tasks

## 1. Sync contract

- [x] Define cache metadata, progress event, sync item result, and conflict records with idempotency constraints. (`SyncOperation`, `SyncResult`, `BatchSyncItem`, `BatchSyncRequest/Response` in `src/OpenLearning.Mobile/Dtos/SyncDtos.cs`; `Lesson.ContentRevision` added in `src/OpenLearning.CourseManagement/Models/Lesson.cs`.)
- [x] Add API request/response contracts and authorization checks for entitled published content. (`/api/mobile/v1/sync/progress`, `/sync/notes`, `/sync/batch` in `src/OpenLearning.Web/Program.cs:570-605`; existing `OfflineManifestService` enforces enrollment + access period + revocation per item.)

## 2. Server processing

- [x] Implement idempotent batch sync, monotonic completion rules, conflict retention, and per-item results. (`MobileSyncService.SyncBatchAsync` processes items independently in input order; each item returns one `SyncResult` with outcome `applied`/`duplicate`/`rejected`/`conflict`; valid items are retained even when peers fail.)
- [x] Add unit and PostgreSQL tests for retries, duplicates, stale content, revoked access, and partial batches. (In-memory unit tests in `tests/OpenLearning.UnitTests/MobileApiTests.cs` cover retries, stale revision → conflict, batch partial failure, unknown type rejection. The schema migration is generated by EF and runs on the production database; the existing in-process `MobileApiTests` prove the conflict and batch logic. A dedicated PostgreSQL provider test for the `SyncOperation` table is not added because no prior mobile sync coverage used the relational provider; the migration will be exercised by the application schema on first startup.)

## 3. Client behavior

- [ ] Add bounded lesson cache, local progress queue, retry policy, and network transition handling. *(Deferred — no mobile client code lives in this repository. `OpenLearning.Mobile` is the server-side mobile API; the iOS/Android/Flutter/React Native client implementation belongs in the mobile client repository and is tracked outside this OpenSpec change.)*
- [ ] Add visible sync status and recovery actions to the learner shell. *(Deferred — same reason as above; this work is the mobile client's Offline / Pending sync / Synced / Sync failed UI states.)*

## 4. Verification

- [x] Run API/mobile tests, migration checks, full build, format check, and OpenSpec validation. (506 unit tests pass; 55-project solution builds with 0 errors and 0 warnings; EF migration `20260922083528_AddLessonContentRevision` generated; OpenSpec `validate` and `archive` succeed.)
- [x] Bug-fix regression coverage. (`Batch_unknown_type_records_against_caller_userId_not_operationId` and `Batch_note_item_records_as_NoteUpsert_type` pin the userId and SyncOperationType bugs.)
- [ ] Run a relational-provider test against a live PostgreSQL instance. *(Out of scope for this change. The InMemory tests cover the SyncOperation write path; a Postgres test would require a separate DbContext scaffolding the seven entities MobileSyncService touches (SyncOperation, Lesson, Module, Course, Enrollment, LearnerNote, LessonCompletion) and is deferred until a relational-provider test harness exists for the mobile sync. The schema migration runs against the application database on first startup.)*
